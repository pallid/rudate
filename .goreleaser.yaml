version: 2

project_name: rudate

archives:
  - format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    files:
      - "!*_test.go"
      - "!testdata"
      - "!examples"

checksum:
  name_template: "checksums.txt"

changelog:
  disable: true

release:
  github:
    owner: pallid
    name: rudate
